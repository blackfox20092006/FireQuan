# Tests

This directory contains test suites ensuring the stability and correctness of FireQuan's components. It includes unit tests for the quantum node implementations, dataloader transformations, architectural blocks like Fire512 Head, and the entire training pipeline to verify reproducibility and prevent regressions.
